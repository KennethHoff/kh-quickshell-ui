// A thin horizontal separator inside a BarDropdown panel.
import QtQuick

Rectangle {
    property color dividerColor: "#313244"

    width:  parent ? parent.width : 200
    height: 1
    color:  dividerColor
}
