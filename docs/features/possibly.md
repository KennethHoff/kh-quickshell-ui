# Possibly

Ideas with clear value but no committed timeline.

## Applications

- **Scratchpad** — floating notepad toggled by keybind; vim bindings
- **Log viewer** — tail `journalctl` or files with unit/level filter

## Plugins

### Bar

- **Ping + bandwidth monitor** — latency to a host plus live throughput
- **Multiple time zones** — extra zones beside the main clock

### Launcher

- **SSH launcher** — fuzzy-search `~/.ssh/config`; opens kitty with `ssh <host>`
- **Web search prefixes** — `g <q>`, `gh <q>`, `mdn <q>`; defined in Nix
- **Browser history** — fuzzy search Firefox/Chromium SQLite history (read-only)
